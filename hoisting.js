

getName() // Namaste Javascript
console.log(x)  //undefine 
console.log(getName) //whole function
var x = 7 
function getName(){ 
    console.log('Namaste Javascript')
}

//if var x is commented them we will have (x is not define) in line 4.
//if getName is arrow function it will treat it like a variable in that case in memory execution an undefine value is stored that is why hoisting is not posible in arrow function.